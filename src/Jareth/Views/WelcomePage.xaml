<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Jareth.Views.WelcomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid VerticalAlignment="Center" HorizontalAlignment="Center" Padding="40">
        <StackPanel Spacing="16" HorizontalAlignment="Center">
            <TextBlock Text="Jareth"
                       FontSize="48"
                       FontWeight="Bold"
                       Foreground="{StaticResource JarethAccentBrush}"
                       HorizontalAlignment="Center" />
            <TextBlock x:Name="GreetingText"
                       FontSize="20"
                       Foreground="Gray"
                       HorizontalAlignment="Center"
                       TextAlignment="Center" />
            <TextBlock Text="Click 'New Recording' to get started"
                       FontSize="14"
                       Foreground="Gray"
                       HorizontalAlignment="Center"
                       Margin="0,20,0,0" />
        </StackPanel>
    </Grid>
</UserControl>
